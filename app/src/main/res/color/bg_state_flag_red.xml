<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Disabled -->
    <item android:color="@android:color/transparent" android:state_enabled="false" />

    <!-- Selected -->
    <item android:color="@color/flag_high_op" android:state_selected="true" />
    <item android:color="@color/flag_high_op" android:state_checked="true" />

    <!-- Not selected -->
    <item android:color="@color/white" />
</selector>